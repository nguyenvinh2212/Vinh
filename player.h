#ifndef player_h
#define player_h

#include "Common.h"
#include "Game_object.h"
/*
class Player : public Entity
{
public:
    int reload;
    Animation* tailAnim;
    Player();
    Player(SDL_Texture* texture_) : tailAnim(nullptr);
    virtual ~Player() ;
    virtual void update();
    void clip();

};
*/
#endif // player_h
